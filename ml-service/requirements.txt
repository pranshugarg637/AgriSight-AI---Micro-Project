# v2: minimum versions (security audit, see docs/privacy.md#security). Floors instead of exact
# pins also avoid the Python 3.13 pin conflicts seen on Windows.
fastapi>=0.141
uvicorn[standard]>=0.53
python-multipart>=0.0.32
pydantic>=2.13
python-dotenv>=1.2.3

torch
torchvision

pillow>=12.3
numpy
scikit-learn

pypdf>=6.19
chromadb
sentence-transformers

httpx>=0.28.1

pytest>=9.1
pytest-asyncio>=1.4
reportlab>=4.2.5
rank_bm25

# Optional (Step 2 translation, Step 5 re-ranker / NLI use sentence-transformers above):
# transformers sentencepiece IndicTransToolkit
